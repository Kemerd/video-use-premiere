# Optional: only needed when you want speaker diarization on the speech lane.
# Get one at https://huggingface.co/settings/tokens with read access.
# Leave blank to skip diarization (faster-whisper still gives word timestamps).
HF_TOKEN=
